<h1><%= @question.title %></h1>
<div><%= @question.body %></div>

<ul>
  <% @question.answers.each do |answer| %>
    <li><%= answer.content %></li>
  <% end %>
</ul>

<%= render partial: "answer/new" locals: { question: @question } %>